{:schema :ac/skill-v4,
 :id :electron-bomb,
 :skill {:category :migrated, :level 1},
 :parameters
 {:damage {:type :double},
  :cooldown-ticks {:type :double},
  :exp-hit {:type :double},
  :settle-ticks {:type :long},
  :settle-ticks-improved {:type :long},
  :improved-exp-threshold {:type :double}},
 :state {},
 :description-key "ability.skill.meltdowner.electron_bomb.desc",
 :name-key "ability.skill.meltdowner.electron_bomb",
 :cooldown {:main {:ticks {:ref [:input :tunables :cooldown-ticks]}}},
 :costs nil,
 :mark-policies nil,
 :requires
 [:caster/id
  :caster/eye
  :caster/aim
  :world/id
  :progression/mastery
  :rng/seed],
 :tunables
 {:damage {:curve :mastery-lerp},
  :cooldown-ticks {:curve :mastery-lerp},
  :exp-hit {:curve :const},
  :settle-ticks {:curve :const, :type :int},
  :settle-ticks-improved {:curve :const, :type :int},
  :improved-exp-threshold {:curve :const}},
 :invariants nil,
 :progression {:cast {:per-mark {:ref [:input :tunables :exp-hit]}}},
 :activation :instant,
 :entry-triggers {:default :activation/start},
 :phases
 {:default
  [(let owner-id ?caster/id)
   (let eye ?caster/eye)
   (let aim ?caster/aim)
   (let wid ?world/id)
   (let mastery ?progression/mastery)
   (let seed ?rng/seed)
   (let cast-progression ?progression/cast)
   (let main-cooldown ?cooldown/main)
   ^{:nid "n/n010"}
   (let __v4_n010 (math/gt mastery $improved-exp-threshold))
   ^{:nid "n/n009"}
   (let
    __v4_n009
    (math/select __v4_n010 $settle-ticks-improved $settle-ticks))
   (let ball-life-ticks __v4_n009)
   ^{:nid "n/n015"}
   (let
    __v4_n015
    (entity/spawn
     {:entity-type "academy:entity_md_ball",
      :add-tags ["ac_electron_bomb"],
      :velocity [0.0 0.0 0.0],
      :barrier? true,
      :world-id wid,
      :owner owner-id,
      :position eye,
      :life-ticks ball-life-ticks}))
   (let spawn-result __v4_n015)
   ^{:nid "n/n020"}
   (let __v4_n020 (:entity-id spawn-result))
   (let ball-id __v4_n020)
   ^{:nid "n/n024"}
   (let __v4_n024 (vec3/scale aim 15.0))
   ^{:nid "n/n022"}
   (let __v4_n022 (vec3/add eye __v4_n024))
   (let destination __v4_n022)
   ^{:nid "n/n029"}
   (let __v4_n029 (long/sub ball-life-ticks 2))
   ^{:nid "n/n--entry--default-12"}
   (projectile/schedule-beam
    {:world-id wid,
     :destination-selector {:mode :owner-raycast, :distance 15.0},
     :instance-key [:activation :electron-bomb-ray],
     :seed seed,
     :damage $damage,
     :damage-type :magic,
     :origin-selector
     {:entity-type "academy:entity_md_ball",
      :required-tags ["ac_electron_bomb"],
      :entity-id ball-id,
      :owner-id owner-id,
      :radius 3.5},
     :delay-ticks __v4_n029,
     :origin eye,
     :settlement-vfx
     {:effect-id :arc-strike-transient,
      :operation :spawn,
      :audience {:type :nearby, :radius 64.0},
      :payload
      {:hand-origin? false,
       :sound-position eye,
       :sound-pitch 1.0,
       :sound-volume 0.0,
       :seed 0,
       :sound-id nil,
       :arc-life-ticks 10,
       :aoe-points [],
       :aoe-origin eye,
       :bounds-radius 0.8,
       :pattern :weak}},
     :destination destination,
     :owner owner-id,
     :exclude-owner? true})
   ^{:nid "n/n--entry--default-13"}
   (event!
    {:type :score/mark,
     :owner owner-id,
     :progression cast-progression})
   ^{:nid "n/n--entry--default-14"}
   (cooldown/start {:name :main, :ticks main-cooldown})
   ^{:nid "n/n--entry--default-15"}
   (finish {:outcome :performed})]}}
