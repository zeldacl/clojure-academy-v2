{:schema :ac/skill-v4,
 :id :mine-detect,
 :skill {:category :migrated, :level 1},
 :activation :instant,
 :parameters
 {:blindness-duration-ticks {:type :long},
  :blindness-amplifier {:type :long},
  :targeting-range {:type :double},
  :cooldown-endpoints {:type :any},
  :exp-cast {:type :double}},
 :state {},
 :tunables
 {:targeting-range {:curve :mastery-lerp},
  :blindness-duration-ticks {:curve :const, :type :int},
  :blindness-amplifier {:curve :const, :type :int},
  :cost-down-cp {:curve :mastery-lerp},
  :cost-down-overload {:curve :mastery-lerp},
  :cooldown-ticks {:curve :mastery-lerp, :type :int},
  :cooldown-endpoints {:curve :pair},
  :exp-cast {:curve :const}},
 :costs
 {:activate
  {:resources {:cp $cost-down-cp, :overload $cost-down-overload}}},
 :cooldown {:main {:ticks $cooldown-ticks}},
 :progression {:cast {:per-mark $exp-cast}},
 :invariants nil,
 :requires
 [:caster/id
  :caster/eye
  :world/id
  :progression/mastery
  :progression/level
  :rng/seed],
 :entry-triggers {:default :activation/start},
 :name-key "ability.skill.electromaster.mine_detect",
 :description-key "ability.skill.electromaster.mine_detect.desc",
 :mark-policies nil,
 :phases
 {:default
  [(let owner-id ?caster/id)
   (let eye ?caster/eye)
   (let mastery ?progression/mastery)
   (let level ?progression/level)
   (let cast-progression ?progression/cast)
   ^{:nid "n/n006"}
   (let __v4_n006 (math/add mastery $exp-cast))
   (let cooldown-exp-next __v4_n006)
   ^{:nid "n/n011"}
   (let __v4_n011 (pair/first $cooldown-endpoints))
   ^{:nid "n/n013"}
   (let __v4_n013 (pair/second $cooldown-endpoints))
   ^{:nid "n/n010"}
   (let __v4_n010 (math/lerp __v4_n011 __v4_n013 cooldown-exp-next))
   ^{:nid "n/n009"}
   (let __v4_n009 (math/floor-long __v4_n010))
   (let cooldown-ticks-next __v4_n009)
   (let activate-budget ?budget/activate)
   ^{:nid "n/n017"}
   (let __v4_n017 (cost/spend {:budget activate-budget}))
   (let sufficient? __v4_n017)
   (if
    sufficient?
    [^{:nid "n/n--entry--default-9-then-0"}
     (combat/status
      {:status-id :blindness,
       :target owner-id,
       :duration-ticks $blindness-duration-ticks,
       :amplifier $blindness-amplifier})
     ^{:nid "n/n--entry--default-9-then-1"}
     (vfx!
      {:effect-id :block-scan-transient,
       :operation :spawn,
       :instance-key [:activation :scan],
       :audience {:type :owner},
       :payload
       {:life-ticks 100,
        :rescan-interval 5,
        :max-results 8400,
        :max-range $targeting-range,
        :filter
        {:include-suffixes ["_ore"],
         :include-substrings ["ancient_debris"],
         :include-tags ["forge:ores"]},
        :origin eye,
        :tier-colors
        [{:harvest-level 0, :color [161 181 188 165]}
         {:harvest-level 1, :color [87 231 248 190]}
         {:harvest-level 2, :color [97 204 94 210]}
         {:harvest-level 3, :color [235 109 84 225]}],
        :base-color [220 235 255 185],
        :texture "academy:textures/effects/mineview.png",
        :advanced?
        ^{:nid "n/n--entry--default-9--1dd1fce9"}
        (bool/and
         ^{:nid "n/n--entry--default-9--cd23ee31"}
         (math/gt mastery 0.5)
         ^{:nid "n/n--entry--default-9--4d2392c9"}
         (math/gte level 4)),
        :range $targeting-range}})
     ^{:nid "n/n--entry--default-9-then-2"}
     (event!
      {:type :score/mark,
       :owner owner-id,
       :progression cast-progression})
     ^{:nid "n/n--entry--default-9-then-3"}
     (cooldown/start {:name :main, :ticks cooldown-ticks-next})
     ^{:nid "n/n--entry--default-9-then-4"}
     (finish {:outcome :performed})]
    [^{:nid "n/n--entry--default-9-else-0"}
     (finish {:outcome :insufficient-resource})])]}}
