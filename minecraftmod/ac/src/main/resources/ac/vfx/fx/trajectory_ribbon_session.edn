{:doc ["New-DSL conversion of ac/vfx/effects/trajectory_ribbon_session.edn"
        "(S6). :vfx/trajectory-ribbon has no sample-node case (confirmed-dead"
        "class, see blood_retrograde_charge.edn's docstring); empty :scene is"
        "the honest port. (Not to be confused with the WORKING vfx-core/"
        "composites/trajectory_ribbon.edn old-tree composite of the same shape"
        "-- that composite is registered under the DIFFERENT id :vfx.fx/"
        "trajectory-ribbon and is never referenced by this ability-level"
        ":vfx/trajectory-ribbon component at all.) Old top-level :bounds"
        "dropped, same reasoning as blood_retrograde_charge.edn."]
 :schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :trajectory-ribbon-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:origin :vec3 :look-dir :vec3 :init-vel :vec3
          :can-perform? :bool :lateral-offset :float
          :forward-offset :float :vertical-offset :float
          :drag :float :gravity :float :dt :float
          :segments :int :width :float :style :any}
  :update {:origin {:type :vec3} :look-dir {:type :vec3}
           :init-vel {:type :vec3} :can-perform? {:type :bool}
           :lateral-offset {:type :float} :forward-offset {:type :float}
           :vertical-offset {:type :float} :drag {:type :float}
           :gravity {:type :float} :dt {:type :float}
           :segments {:type :int} :width {:type :float}
           :style {:type :any}}
  :destroy {}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :trajectory-ribbon-session
   :do [(finish {:outcome :performed})]}"}
