{:doc ["New-DSL conversion of ac/vfx/effects/block_progress_session.edn (S6)."
        ":vfx/block-progress has no sample-node case (confirmed-dead class, see"
        "blood_retrograde_charge.edn's docstring); empty :scene is the honest"
        "port."]
 :schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :block-progress-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:target :vec3 :progress :float :color :any
          :pulse-period {:type :float :default 0.3}}
  :update {:target {:type :vec3} :progress {:type :float}
           :color {:type :any} :pulse-period {:type :float}}
  :destroy {:fade-ticks :int}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :block-progress-session
   :do [(finish {:outcome :performed})]}"}
