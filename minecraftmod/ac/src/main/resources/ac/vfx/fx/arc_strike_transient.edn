{:doc ["New-DSL conversion of ac/vfx/effects/arc_strike_transient.edn (S6)."
        ":vfx/arc-strike has no sample-node case (confirmed-dead class, see"
        "blood_retrograde_charge.edn's docstring); empty :scene is the honest"
        "port. Old top-level :bounds dropped, same reasoning."]
 :schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :arc-strike-transient
 :revision 1
 :lifecycle :transient
 :inputs
 {:spawn {:start :vec3 :end :vec3 :aoe-origin :vec3 :aoe-points :any
          :arc-life-ticks :int :pattern :resource-id :hand-origin? :bool
          :sound-id :string :sound-volume :float :sound-pitch :float
          :sound-position :vec3 :bounds-radius :float}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :arc-strike-transient
   :do [(finish {:outcome :performed})]}"}
