;; New-DSL conversion of ac/vfx/effects/target_mark_session.edn (S6).
;; :vfx/mark-sparks has no sample-node case (confirmed-dead class, see
;; blood_retrograde_charge.edn's docstring); empty :scene is the honest
;; port. Old top-level :bounds dropped, same reasoning.
{:schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :target-mark-session
 :revision 1
 :lifecycle :transient
 :inputs
 {:spawn {:position :vec3
          :ttl-ticks :int
          :count :int
          :radius :float
          :color :any
          :seed :int}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :target-mark-session
   :do [(finish {:outcome :performed})]}"}
