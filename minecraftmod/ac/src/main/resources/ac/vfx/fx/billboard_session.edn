;; New-DSL conversion of ac/vfx/effects/billboard_session.edn (S6).
;; :vfx/billboard-sequence has no sample-node case (confirmed-dead class,
;; see blood_retrograde_charge.edn's docstring); empty :scene is the
;; honest port.
{:schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :billboard-session
 :revision 1
 :lifecycle :session
 :inputs {:spawn {:anchor :any :duration-ticks :int :texture-pattern :string
                  :frame-count :int :frame-duration-ms :int :half-size :float}
          :update {:anchor :any}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :billboard-session
   :do [(finish {:outcome :performed})]}"}
