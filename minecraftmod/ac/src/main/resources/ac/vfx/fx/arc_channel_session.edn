{:doc ["New-DSL conversion of ac/vfx/effects/arc_channel_session.edn (S6)."
        ":vfx/channel-arc has no sample-node case (confirmed-dead class, see"
        "blood_retrograde_charge.edn's docstring); empty :scene is the honest"
        "port. Old top-level :bounds dropped, same reasoning."]
 :schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :arc-channel-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:mode :resource-id :caster :vec3 :target :any
          :block-pos :any :block-bounds :any :good? :bool
          :charge-ticks :int :visual-max-ticks :int :style :any}
  :update {:mode :resource-id :caster :vec3 :target :any
           :block-pos :any :block-bounds :any :good? :bool
           :charge-ticks :int :visual-max-ticks :int :style :any}
  :destroy {:fade-ticks :int}}
 :state-slots {:age :float :seed :int}
 :scene
 "{:ability :arc-channel-session
   :do [(finish {:outcome :performed})]}"}
