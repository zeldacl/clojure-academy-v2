{:schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :trajectory-ribbon-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:origin :vec3 :look-dir :vec3 :init-vel :vec3
          :can-perform? :bool :lateral-offset :float
          :forward-offset :float :vertical-offset :float
          :drag :float :gravity :float :dt :float
          :segments :int :width :float :style :any}
  :update {:origin {:type :vec3} :look-dir {:type :vec3}
           :init-vel {:type :vec3} :can-perform? {:type :bool}
           :lateral-offset {:type :float} :forward-offset {:type :float}
           :vertical-offset {:type :float} :drag {:type :float}
           :gravity {:type :float} :dt {:type :float}
           :segments {:type :int} :width {:type :float}
           :style {:type :any}}
  :destroy {}}
 :state-slots {:age :float :seed :int}
 :control-graph {:component :vfx/trajectory-ribbon
         :origin {:ref [:input :origin]}
         :initial-velocity {:ref [:input :init-vel]}
         :look-dir {:ref [:input :look-dir]}
         :lateral-offset {:ref [:input :lateral-offset]}
         :forward-offset {:ref [:input :forward-offset]}
         :vertical-offset {:ref [:input :vertical-offset]}
         :drag {:ref [:input :drag]}
         :gravity {:ref [:input :gravity]}
         :dt {:ref [:input :dt]}
         :segments {:ref [:input :segments]}
         :width {:ref [:input :width]}
         :style {:ref [:input :style]}
         :can-perform? {:ref [:input :can-perform?]}}
 :bounds {:component :vfx/beam-bounds
          :start {:ref [:input :origin]}
          :end {:ref [:input :origin]}
          :radius 16.0}}

