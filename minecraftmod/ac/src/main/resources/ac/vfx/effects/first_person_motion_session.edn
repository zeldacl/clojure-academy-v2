{:schema-version 2
 :kind :vfx-effect
 :id :first-person-motion-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:stage :keyword
          :phase-ticks :long
          :duration-ticks :long
          :curves :object}
  :update {:stage :keyword
           :phase-ticks :long
           :duration-ticks :long}
  :destroy {}}
 :state-slots {:age :double :seed :long}
 :graph {:component :vfx/first-person-motion
         :stage {:ref [:input :stage]}
         :phase-ticks {:ref [:input :phase-ticks]}
         :duration-ticks {:ref [:input :duration-ticks]}
         :curves {:ref [:input :curves]}}}
