{:schema-version 2
 :kind :vfx/system
 :asset/type :vfx/system
 :asset/version 1
 :id :first-person-motion-session
 :revision 1
 :lifecycle :session
 :inputs
 {:spawn {:stage :resource-id
          :phase-ticks :int
          :duration-ticks :int
          :curves :any}
  :update {:stage :resource-id
           :phase-ticks :int
           :duration-ticks :int}
  :destroy {}}
 :state-slots {:age :float :seed :int}
 :control-graph {:component :vfx/first-person-motion
         :stage {:ref [:input :stage]}
         :phase-ticks {:ref [:input :phase-ticks]}
         :duration-ticks {:ref [:input :duration-ticks]}
         :curves {:ref [:input :curves]}}}

