(ns cn.li.ac.block.phase-gen.gui
  "Phase Generator GUI - client-side only"
  (:require [cn.li.mcmod.util.log :as log]))

;; GUI implementation will be added when needed
;; For now, just a placeholder namespace

(defn init-phase-gen-gui!
  []
  (log/info "Phase Generator GUI initialized"))
