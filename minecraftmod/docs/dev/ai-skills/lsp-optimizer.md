---
name: lsp-navigator
description: 混合语言项目语义导航规范
---

# LSP Navigator Skill

## 核心准则

1. 定位优先：找定义优先使用语言服务器（Clojure/Java LSP）。
2. 片段读取：优先读取最小必要范围，避免整文件无差别读取。
3. 混合工程同步：跨子模块无法解析时，先执行对应 `:classes` 刷新索引。

## 实践建议

- 搜索符号前先明确语言与模块边界。
- 文本搜索用于过滤，定义跳转用于确认事实。
- 大文件优先按函数/命名空间分块阅读。
