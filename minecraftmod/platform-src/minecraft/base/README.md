# Minecraft base component

Shared Minecraft API adaptation belongs here when it applies across supported Minecraft versions.

Keep version-specific API differences under `platform-src/minecraft/version/<mc-component>/`.
