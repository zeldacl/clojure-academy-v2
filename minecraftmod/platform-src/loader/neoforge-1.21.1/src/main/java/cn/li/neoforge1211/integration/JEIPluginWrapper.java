package cn.li.neoforge1211.integration;

/**
 * JEI is not yet a declared capability/dependency for the neoforge-1.21.1 target.
 * Keep this placeholder so the package remains stable; restore the real
 * {@code @JeiPlugin} wrapper when JEI NeoForge artifacts are added to the catalog.
 */
public final class JEIPluginWrapper {
    private JEIPluginWrapper() {
    }
}
